export const tabs = state => state.tabs;

export const newsCategoryId = state => state.newsCategoryId;

export const mesCount = state => state.mesCount;

export const areaCode = state => state.areaCode;
